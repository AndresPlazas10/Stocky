import type {
  ComboItemRecord,
  ComboProductRecord,
  ComboRecord,
  ComboStatus,
  ComboUpsertPayload,
} from '../../services/combosService';

export type CombosStatus = ComboStatus;
export type CombosListItem = ComboRecord;
export type CombosItemRecord = ComboItemRecord;
export type CombosProductItem = ComboProductRecord;
export type CombosUpsertPayload = ComboUpsertPayload;
