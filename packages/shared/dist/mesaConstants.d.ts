/** Ventana en ms en la que un call_requested_at se considera "activo" (cocina llamando). */
export declare const CALL_WINDOW_MS: number;
/** TTL del lock de edición de mesa (segundos). */
export declare const MESA_LOCK_TTL_SECONDS = 45;
/** TTL del lock de edición de mesa (ms). */
export declare const MESA_LOCK_TTL_MS: number;
/** Intervalo del heartbeat de renovación del lock de edición (ms). */
export declare const MESA_LOCK_HEARTBEAT_MS = 20000;
/** Intervalo del poll de fallback de mesas (ms). */
export declare const MESAS_REMOTE_FALLBACK_POLL_MS = 5000;
//# sourceMappingURL=mesaConstants.d.ts.map